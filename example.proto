// example file

//- this is test input
message Test 
{
  //- this is map extended data
  map<string, string> data = 1;
  //- this is repeat field
  required string hello = 2;
  oneof test {
    uint32 age = 3;
    uint32 year = 4;
  }
  //- this is a nested field
  message Nested {
    optional bytes thing = 1;
  }
}
enum A {
    //- this is enum A1 output
    A1 = 1;
    A2 = 2;
}
